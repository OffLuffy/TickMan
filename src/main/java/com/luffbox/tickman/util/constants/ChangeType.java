package com.luffbox.tickman.util.constants;

public class ChangeType {
    public enum Dept {
        ROLES, CATEGORY, CHANNEL, NAME
    }
    public enum Ticket {
        AUTHOR, SUBJECT, CHANNEL
    }
}
